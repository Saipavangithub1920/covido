apiVersion: v1
kind: PersistentVolume
metadata:
  name: covidomysqlpv
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: covidostorageclass
  hostPath:
    path: /u01/data